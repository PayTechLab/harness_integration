meta {
  name: Create_User_ID
  type: http
  seq: 2
}

post {
  url: https://reqres.in/api/users
  body: json
  auth: inherit
}

body:json {
  {
    "name": "{{userName}}",
    "email": "{{email}}"
  }
}

script:post-response {
  bru.setEnvVar("userId", res.body.id);
}
